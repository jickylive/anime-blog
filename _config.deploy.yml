# Hexo 部署配置 - hxfund.cn/blog (虚拟主机子目录)
# 此文件包含敏感信息，已添加到 .gitignore，请勿提交到 Git

deploy:
  type: ftpsync
  host: ${FTP_HOST}
  user: ${FTP_USER}
  pass: ${FTP_PASSWORD}
  port: ${FTP_PORT}
  remote: ${FTP_REMOTE}  # /htdocs/public/blog
  clear: false
  verbose: true
